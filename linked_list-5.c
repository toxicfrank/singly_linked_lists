#include <stdio.h>
#include <stdlib.h>

typedef struct node
{
    int x;
    struct node *next;
}node;

/**
 * main - easier method to add elements in a linked
 * list
 * Return: always 0
 */

int main(int argc, char **argv)
{
    

    return (0);
}
